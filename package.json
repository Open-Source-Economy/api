{
  "name": "@open-source-economy/api-types",
  "version": "1.3.0",
  "description": "Shared TypeScript types for Open Source Economy website API.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "fmt": "prettier */*.js \"*/**/*{.js,.ts,.tsx}\" -w ; tsc --project tsconfig.json && tsc --project tsconfig.test.json ; rm -f -r dist"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.3.0",
    "@types/stripe": "^8.0.416",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "decimal.js": "^10.6.0",
    "http-status-codes": "^2.3.0",
    "joi": "^18.0.1",
    "stripe": "^18.4.0"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  }
}
